import Root, { type HeaderProps } from './Header.svelte';
import Navigation, { type NavigationProps } from './Navigation.svelte';
import Footer, { type FooterProps } from './Footer.svelte';

export {
	Root,
	Root as Header,
	type HeaderProps,
	Navigation,
	type NavigationProps,
	Footer,
	type FooterProps
};
